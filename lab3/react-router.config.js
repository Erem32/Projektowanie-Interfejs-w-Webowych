// react-router.config.js
/**
 * @type {import('@react-router/dev/config').Config}
 */
export default {
  // points the plugin at your `app/` folder
  appDirectory: "app",

  // completely disables any SSR entry/runtime requirement
  ssr: false,
};
